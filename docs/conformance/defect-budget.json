{
  "_comment": "Per-suite ceilings for defects that are NOT ordinary failures. A crash, a hang and a file the parser cannot read are each a different thing from 'the runtime returned the wrong answer', and averaged into a four-digit fail count all three are invisible. tools/check-defect-budget.mjs enforces these against the committed report.",
  "_directions": "crashes is exact (it must match, in both directions — they are rare and individually known). hangs and parseFailures are ceilings: they may drop without ceremony, but never rise. hangs, not timeouts: a 10s timeout under a loaded pool is partly a statement about the machine, and the sweep's confirm pass re-runs every timed-out case at 45s so the ratcheted number reproduces.",
  "node-compat": {
    "crashes": 2,
    "hangs": 157,
    "parseFailures": 5,
    "known": [
      "crash: test-signal-args.js sends itself SIGINT; milojs has no process.on('SIGINT'), so the default action kills it",
      "crash: test-signal-handler.js, same with SIGHUP",
      "hangs: 157 cases still time out at 45s in the confirm pass, so they are wedged, not slow. The raw 10s timeout count sits a few above this and is NOT ratcheted: it moved 162/164/163 across three runs of engines differing by one fix, because a handful of merely-slow cases cross the 10s line depending on machine load."
    ]
  },
  "test262": {
    "crashes": 1,
    "parseFailures": 23,
    "known": [
      "crash: built-ins/Map/iterator-item-first-entry-returns-abrupt.js HANGS -- SIGTERM from the harness timeout, not a segfault. Reproduces standalone: harness + case under .dev/mj-engine never returns. An abrupt completion from the first entry's iterator is not unwinding the Map construction loop.",
      "parseFailures: 23 of 292 failures are syntax the parser does not accept; the rest are semantics."
    ]
  },
  "quickjs": {
    "parseFailures": 0,
    "crashes": 2,
    "known": [
      "crashes: 2 SIGTERM cases -- the harness timeout, so hangs rather than segfaults. Both still need reductions that tell an engine loop apart from a legitimately slow path.",
      "parseFailures is 0 and must stay 0: the quickjs corpus is small enough that one missing syntax feature takes several whole files with it."
    ]
  }
}
