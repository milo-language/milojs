// @error: expected ExprId, got ObjLitId

from "../../src/engine/ast" import {
    PropInit, addObjLit, getExpr, newProg
}

fn confuseObjectLiteralAndExpression(): void {
    var prog = newProg()
    var properties: Vec<PropInit> = Vec.new()
    let literalId = addObjLit(prog, properties)
    let _ = getExpr(prog, literalId)
}
